#!/bin/bash

function addition(){
	result=$(($1 + $2))
}

function subtraction(){
	result=$(($1 - $2))
}

function division(){
	result=$(($1 / $2))
}

function multiplication(){
	result=$(($1 * $2))
}